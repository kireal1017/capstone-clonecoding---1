# JWT Secrets (use strong random strings in production)
JWT_ACCESS_SECRET=replace-me-with-strong-access-secret
JWT_REFRESH_SECRET=replace-me-with-different-strong-refresh-secret

# Database
DATABASE_URL="file:./prisma/dev.db"

# Server
PORT=4000
NODE_ENV=development

# CORS
FRONT_ORIGIN=http://localhost:5173
